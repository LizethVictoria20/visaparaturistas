# from app import app, db, User

# with app.app_context():
#     db.create_all()
#     admin = User(username='visa2', password='visa1245', roles="admin")
#     db.session.add(admin)
#     db.session.commit()
